// Root-level next.config.js
//
// This file exists solely so Vercel's framework detector recognises the repo
// as a Next.js project (belt-and-suspenders alongside `next` in package.json).
//
// The actual Next.js application lives in ready-layer/.
// vercel.json overrides buildCommand to build from there.

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
