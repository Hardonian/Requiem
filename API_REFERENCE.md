# API Reference (Canonical Entry)

Canonical API contracts are defined in:

- `ready-layer/src/app/api/**/route.ts`
- `routes.manifest.json`
- `docs/CONTRACT.md`

Regenerate and verify route truth:

- `pnpm run route:inventory`
- `pnpm run verify:routes`
