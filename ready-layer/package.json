{
  "name": "ready-layer",
  "version": "0.8.0",
  "private": true,
  "description": "Requiem Enterprise Cloud dashboard — ready-layer.com",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "verify": "npm run typecheck && npm run lint",
    "verify:routes": "bash ../scripts/verify_routes.sh",
    "verify:boundaries": "bash ../scripts/verify_boundaries.sh",
    "verify:deps": "bash ../scripts/verify_deps.sh",
    "verify:migrations": "bash ../scripts/verify_migrations.sh"
  },
  "dependencies": {
    "next": "14.2.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^18",
    "typescript": "^5"
  }
}
