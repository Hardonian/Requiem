# =============================================================================
# Supabase Configuration
# =============================================================================
# These variables are required for the Vector Search subsystem.

# Supabase Project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase anon key (public, for client-side use)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase service role key (secret, for server-side use only)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# =============================================================================
# Database Connection (Supabase session pooler)
# =============================================================================
# DATABASE_URL - For app runtime (via Supabase session pooler)
# Format: postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:6543/postgres
DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:6543/postgres

# DIRECT_DATABASE_URL - For migrations (direct Postgres connection)
# Format: postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres
DIRECT_DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# =============================================================================
# ReadyLayer / Requiem Engine Configuration
# =============================================================================
# Auth secret for token validation
REQUIEM_AUTH_SECRET=your-auth-secret-here

# Node API URL (for engine communication)
REQUIEM_API_URL=http://localhost:3001

# Public app URL (for CORS / server action origins)
NEXT_PUBLIC_APP_URL=http://localhost:3000
