# Documentation Governance

Canonical docs live at repository root for entrypoints and under `docs/` for detail.

Rules:

- Root docs are index/canonical pointers.
- Historical/one-off reports belong in `docs/archive/`.
- Claims must map to tests/scripts before they are stated as guarantees.
