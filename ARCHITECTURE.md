# Architecture (Canonical Entry)

Canonical architecture reference lives in `docs/ARCHITECTURE.md`.

Fast verification:

- `pnpm run verify:routes`
- `pnpm run verify:contracts`
- `pnpm run verify:tenant-isolation`
