/**
 * @fileoverview Datastore tool stubs.
 *
 * These tools wrap CLI/DB operations as AI-accessible tools.
 * Currently stubbed — wire to actual repositories when packages/cli is connected.
 *
 * TODO: Import from @requiem/cli when workspace deps are configured.
 */

// Stubs intentionally left minimal — no cross-package imports to avoid circular deps.
// Real implementations should be registered by the application layer, not this package.
export {};
