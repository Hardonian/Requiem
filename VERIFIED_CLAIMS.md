# Verified Claims (Canonical Entry)

Canonical evidence mapping lives in `docs/VERIFIED_CLAIMS.md`.

Recent launch-hardening proofs:

- Route manifest drift prevention: `scripts/route-inventory.ts`, `scripts/verify-routes.ts`
- Auth fail-closed behavior: `ready-layer/tests/auth-enforcement.test.ts`
- Tenant isolation proofs: `ready-layer/tests/tenant-isolation-api.test.ts`
