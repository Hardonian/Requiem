# Force installation of devDependencies for verification scripts
public-hoist-pattern[]=*

# Handle native dependencies - never build these during install
neverBuiltDependencies=better-sqlite3

# Ensure consistent peer dependency resolution
auto-install-peers=true

# Disable frozen lockfile in CI to handle config mismatches
# (Vercel sets this automatically, but we ensure it)
