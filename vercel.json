{
  "framework": "nextjs",
  "rootDirectory": "ready-layer",
  "buildCommand": "cd packages/ai && npm install --ignore-scripts --include=dev && npm run build && cd ../ready-layer && npm install --legacy-peer-deps && npm install --legacy-peer-deps file:../packages/ai && npx next build",
  "outputDirectory": ".next"
}
