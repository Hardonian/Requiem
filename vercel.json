{
  "framework": "nextjs",
  "buildCommand": "cd ready-layer && npm install && npx next build",
  "outputDirectory": "ready-layer/.next",
  "installCommand": "echo 'skipping root install — handled in buildCommand'"
}
