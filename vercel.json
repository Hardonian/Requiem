{
  "framework": "nextjs",
  "buildCommand": "cd ready-layer && npm install --ignore-scripts && npx next build",
  "outputDirectory": "ready-layer/.next",
  "installCommand": "npm install --no-package-lock"
}
