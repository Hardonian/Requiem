# SDK Overview

Requiem exposes SDK lanes in:

- `sdk/typescript`
- `sdk/python`
- `sdk/go`

Each SDK lane is scoped to support:

- execute workflows
- retrieve proofpacks
- replay executions
- register adapters
- query execution history

These directories are bootstrapped for implementation and integration with existing proof and execution primitives.
