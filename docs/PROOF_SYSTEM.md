# Proof System

The proof system binds execution identity to deterministic replay and storage integrity.

Core proof surfaces:
- execution fingerprinting
- replay diff
- policy snapshot linkage
- CAS/WAL graph exports
