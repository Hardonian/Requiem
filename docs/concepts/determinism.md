# Determinism

Determinism means identical input contracts produce identical digests and verifiable replay output.

ReadyLayer operators should prefer replay mode with deterministic timestamps for proof generation.
