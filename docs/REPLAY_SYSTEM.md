# Replay System

Replay uses recorded execution artifacts to deterministically reconstruct decision paths.

Operator surfaces:
- `requiem replay <bundle>`
- `requiem replay run <id>`
- `requiem replay export <id> --format graph-json`
- `requiem diff replay <runA> <runB>`
